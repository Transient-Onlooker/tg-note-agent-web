{
  "name": "@note-relay/worker",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "generate-types": "wrangler types",
    "typecheck": "npm run generate-types && tsc --noEmit"
  },
  "dependencies": {
    "hono": "^4.13.5"
  },
  "devDependencies": {
    "typescript": "^7.0.2",
    "wrangler": "^4.126.0"
  }
}
